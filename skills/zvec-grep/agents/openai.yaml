interface:
  display_name: "zvec-grep"
  short_description: "Repository search, indexing, and daemon diagnostics"
  default_prompt: "Use $zvec-grep to investigate this repository before raw grep or rg, selecting an available native MCP tool first and falling back to the zg CLI when needed."
